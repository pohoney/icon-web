window.TENNIS_ICON_CONFIG = {
  // EdgeOne backend origin, for example: "https://icon-web-backstage.edgeone.cool".
  // Leave empty when the frontend and backend are deployed together on the same domain.
  apiBaseUrl: "https://imagecopilot.edgeone.cool",
  // Supabase project URL, for example: "https://xxxx.supabase.co"
  supabaseUrl: "",
  // Supabase anon/public key. This is safe to expose when RLS is configured.
  anonKey: ""
};
